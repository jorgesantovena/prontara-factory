/* Tailwind retirado el 28/04/2026. Si en el futuro se quiere reintroducir,
   añadir "@tailwindcss/postcss" aquí + reinstalar tailwindcss. */
const config = {
  plugins: {},
};

export default config;
